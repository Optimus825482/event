# Etkinlik Ekip Organizasyonu - Teknik Tasarım

## 🏛️ Mimari Genel Bakış

```
┌─────────────────────────────────────────────────────────────┐
│                    TeamOrganizationPage                      │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                   WizardHeader                          ││
│  │  [Event Info] [Step 1] [Step 2] [Step 3] [Step 4] [5]   ││
│  └─────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────┐│
│  │                   WizardContent                         ││
│  │  ┌─────────────────────────────────────────────────────┐││
│  │  │              Active Step Component                  │││
│  │  │  (Step1 | Step2 | Step3 | Step4 | Step5)           │││
│  │  └─────────────────────────────────────────────────────┘││
│  └─────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────┐│
│  │                  WizardNavigation                       ││
│  │  [Geri]                              [İleri / Kaydet]   ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

## 📦 Component Hiyerarşisi

```
TeamOrganizationPage (page.tsx)
├── WizardHeader
│   ├── EventInfo
│   └── WizardStepper
├── WizardContent
│   ├── Step1TableGrouping
│   │   ├── CanvasRenderer
│   │   │   ├── TableItem (memo)
│   │   │   └── LocaItem (memo)
│   │   ├── GroupList
│   │   │   └── GroupCard (memo)
│   │   └── GroupFormModal
│   ├── Step2TeamDefinition
│   │   ├── TeamList
│   │   │   └── TeamDefinitionCard (memo)
│   │   ├── RoleRequirementForm
│   │   └── TeamFormModal
│   ├── Step3GroupAssignment
│   │   ├── UnassignedGroupsPanel
│   │   ├── TeamsPanel
│   │   │   └── TeamWithGroups (memo)
│   │   └── DragDropContext
│   ├── Step4StaffAssignment
│   │   ├── StaffPool
│   │   │   └── StaffCard (memo)
│   │   ├── TeamStaffPanel
│   │   │   └── TeamStaffList (memo)
│   │   └── ShiftSelector
│   └── Step5Summary
│       ├── SummaryStats
│       ├── TeamBreakdown
│       ├── WarningsList
│       └── ExportButtons
└── WizardNavigation
```

## 🔄 State Akışı

```
┌──────────────────────────────────────────────────────────────┐
│                      useWizardState                          │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ currentStep: WizardStep                                │  │
│  │ tableGroups: TableGroup[]                              │  │
│  │ teams: TeamDefinition[]                                │  │
│  │ hasChanges: boolean                                    │  │
│  └────────────────────────────────────────────────────────┘  │
│                           │                                   │
│                           ▼                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ Actions:                                               │  │
│  │ - goNext(), goPrev(), goToStep()                       │  │
│  │ - addTableGroup(), updateTableGroup(), deleteGroup()   │  │
│  │ - addTeam(), updateTeam(), deleteTeam()                │  │
│  │ - assignGroupToTeam(), unassignGroup()                 │  │
│  │ - assignStaffToTeam(), unassignStaff()                 │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│                    useOrganizationData                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ event: EventData                                       │  │
│  │ tables: TableData[]                                    │  │
│  │ allStaff: Staff[]                                      │  │
│  │ loading, error                                         │  │
│  └────────────────────────────────────────────────────────┘  │
│                           │                                   │
│                           ▼                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ API Methods:                                           │  │
│  │ - loadData()                                           │  │
│  │ - saveOrganization()                                   │  │
│  │ - reload()                                             │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## 📐 Step Component Detayları

### Step 1: Masa Gruplandırma

```typescript
interface Step1Props {
  tables: TableData[];
  tableGroups: TableGroup[];
  canvasDimensions: { width: number; height: number };
  onAddGroup: (name: string, tableIds: string[], color?: string) => TableGroup;
  onUpdateGroup: (groupId: string, updates: Partial<TableGroup>) => void;
  onDeleteGroup: (groupId: string) => void;
}

// Layout:
// ┌─────────────────────────────────────────────────────────┐
// │ [Toolbar: Zoom, Pan, Reset, Auto-Group]                 │
// ├─────────────────────────────────┬───────────────────────┤
// │                                 │   Gruplar             │
// │         CANVAS                  │   ┌─────────────────┐ │
// │    (Lasso Selection)            │   │ Grup 1 (5 masa) │ │
// │                                 │   │ Grup 2 (8 masa) │ │
// │                                 │   │ + Yeni Grup     │ │
// │                                 │   └─────────────────┘ │
// └─────────────────────────────────┴───────────────────────┘
```

### Step 2: Takım Tanımlama

```typescript
interface Step2Props {
  teams: TeamDefinition[];
  onAddTeam: (name: string, color?: string) => TeamDefinition;
  onUpdateTeam: (teamId: string, updates: Partial<TeamDefinition>) => void;
  onDeleteTeam: (teamId: string) => void;
  onAddStaffRequirement: (
    teamId: string,
    role: StaffRole,
    count: number
  ) => void;
  onRemoveStaffRequirement: (teamId: string, role: StaffRole) => void;
}

// Layout:
// ┌─────────────────────────────────────────────────────────┐
// │ [+ Yeni Takım]                                          │
// ├─────────────────────────────────────────────────────────┤
// │ ┌─────────────────────────────────────────────────────┐ │
// │ │ 🔵 A Takımı                              [Düzenle]  │ │
// │ │ Gerekli Personel:                                   │ │
// │ │ • Garson: 4 kişi                                    │ │
// │ │ • Komi: 2 kişi                                      │ │
// │ │ [+ Rol Ekle]                                        │ │
// │ └─────────────────────────────────────────────────────┘ │
// │ ┌─────────────────────────────────────────────────────┐ │
// │ │ 🟢 B Takımı                              [Düzenle]  │ │
// │ │ ...                                                 │ │
// │ └─────────────────────────────────────────────────────┘ │
// └─────────────────────────────────────────────────────────┘
```

### Step 3: Grup-Takım Eşleştirme

```typescript
interface Step3Props {
  tableGroups: TableGroup[];
  teams: TeamDefinition[];
  onAssignGroupToTeam: (groupId: string, teamId: string) => void;
  onUnassignGroup: (groupId: string) => void;
}

// Layout:
// ┌─────────────────────────────────────────────────────────┐
// │ Atanmamış Gruplar          │    Takımlar               │
// ├────────────────────────────┼────────────────────────────┤
// │ ┌────────────────────────┐ │ ┌────────────────────────┐ │
// │ │ 📦 Grup 1 (5 masa)     │ │ │ 🔵 A Takımı            │ │
// │ │ [Drag to assign]       │ │ │ ├─ Grup 3 (8 masa)     │ │
// │ └────────────────────────┘ │ │ └─ Grup 4 (6 masa)     │ │
// │ ┌────────────────────────┐ │ │ [Drop zone]            │ │
// │ │ 📦 Grup 2 (4 masa)     │ │ └────────────────────────┘ │
// │ └────────────────────────┘ │ ┌────────────────────────┐ │
// │                            │ │ 🟢 B Takımı            │ │
// │                            │ │ [Drop zone]            │ │
// │                            │ └────────────────────────┘ │
// └────────────────────────────┴────────────────────────────┘
```

### Step 4: Personel Atama

```typescript
interface Step4Props {
  teams: TeamDefinition[];
  allStaff: Staff[];
  onAssignStaff: (teamId: string, role: StaffRole, staffId: string) => void;
  onUnassignStaff: (teamId: string, role: StaffRole, staffId: string) => void;
}

// Layout:
// ┌─────────────────────────────────────────────────────────┐
// │ [Arama] [Filtre: Pozisyon]                              │
// ├────────────────────────────┬────────────────────────────┤
// │ Personel Havuzu            │    Takım Atamaları         │
// │ ┌────────────────────────┐ │ ┌────────────────────────┐ │
// │ │ 👤 Ahmet Yılmaz        │ │ │ 🔵 A Takımı            │ │
// │ │    Garson              │ │ │ Garson: 2/4 ✓          │ │
// │ └────────────────────────┘ │ │ • Mehmet K.            │ │
// │ ┌────────────────────────┐ │ │ • Ayşe T.              │ │
// │ │ 👤 Mehmet Kaya         │ │ │ Komi: 1/2 ⚠           │ │
// │ │    Komi                │ │ │ • Ali V.               │ │
// │ └────────────────────────┘ │ └────────────────────────┘ │
// └────────────────────────────┴────────────────────────────┘
```

### Step 5: Özet & Kaydet

```typescript
interface Step5Props {
  tableGroups: TableGroup[];
  teams: TeamDefinition[];
  stats: WizardStats;
  onSave: () => Promise<boolean>;
  onExportPDF: () => void;
  onExportExcel: () => void;
  onSaveAsTemplate: (name: string) => Promise<boolean>;
}

// Layout:
// ┌─────────────────────────────────────────────────────────┐
// │ 📊 Özet İstatistikler                                   │
// │ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
// │ │ 4 Takım  │ │ 12 Grup  │ │ 48 Masa  │ │ 24 Kişi  │    │
// │ └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
// ├─────────────────────────────────────────────────────────┤
// │ ⚠️ Uyarılar                                             │
// │ • A Takımı: 2 garson eksik                              │
// │ • Grup 5: Takıma atanmamış                              │
// ├─────────────────────────────────────────────────────────┤
// │ Takım Detayları                                         │
// │ ┌─────────────────────────────────────────────────────┐ │
// │ │ 🔵 A Takımı - 14 masa, 6 personel                   │ │
// │ │ Gruplar: 1-5, 6-10, 11-14                           │ │
// │ │ Personel: Ahmet (G), Mehmet (G), Ali (K)...         │ │
// │ └─────────────────────────────────────────────────────┘ │
// ├─────────────────────────────────────────────────────────┤
// │ [PDF] [Excel] [Şablon Kaydet]              [💾 KAYDET] │
// └─────────────────────────────────────────────────────────┘
```

## 🎨 Shared Components

### CanvasRenderer

```typescript
interface CanvasRendererProps {
  tables: TableData[];
  tableGroups: TableGroup[];
  selectedTableIds: string[];
  zoom: number;
  offset: { x: number; y: number };
  onTableClick: (tableId: string, e: React.MouseEvent) => void;
  onLassoSelect: (tableIds: string[]) => void;
  onZoomChange: (zoom: number) => void;
  onOffsetChange: (offset: { x: number; y: number }) => void;
  readOnly?: boolean;
}
```

### GroupCard

```typescript
interface GroupCardProps {
  group: TableGroup;
  tableCount: number;
  isSelected?: boolean;
  isDragging?: boolean;
  onEdit?: () => void;
  onDelete?: () => void;
  onClick?: () => void;
}
```

### TeamCard

```typescript
interface TeamCardProps {
  team: TeamDefinition;
  assignedGroups: TableGroup[];
  assignedStaff: Staff[];
  isDropTarget?: boolean;
  onEdit?: () => void;
  onDelete?: () => void;
  onDropGroup?: (groupId: string) => void;
  onDropStaff?: (staffId: string, role: StaffRole) => void;
}
```

## 🔧 Utility Functions

```typescript
// canvas-helpers.ts
export function calculateLassoSelection(
  start: Point,
  end: Point,
  tables: TableData[]
): string[];

export function generateAutoGroupName(
  tableIds: string[],
  tables: TableData[]
): string;

export function getNextGroupColor(existingGroups: TableGroup[]): string;

// validation.ts
export function validateStep1(tableGroups: TableGroup[]): ValidationResult;
export function validateStep2(teams: TeamDefinition[]): ValidationResult;
export function validateStep3(
  tableGroups: TableGroup[],
  teams: TeamDefinition[]
): ValidationResult;
export function validateStep4(teams: TeamDefinition[]): ValidationResult;

// export-helpers.ts
export function exportToPDF(data: ExportData): void;
export function exportToExcel(data: ExportData): void;
```

## 📋 Implementation Tasks

### Phase 1: Foundation (Temel Yapı)

- [ ] Task 1.1: Dosya yapısını oluştur
- [ ] Task 1.2: Shared types güncelle
- [ ] Task 1.3: useCanvasInteraction hook
- [ ] Task 1.4: CanvasRenderer component
- [ ] Task 1.5: WizardNavigation component

### Phase 2: Step Components

- [ ] Task 2.1: Step1TableGrouping (refactor mevcut)
- [ ] Task 2.2: Step2TeamDefinition
- [ ] Task 2.3: Step3GroupAssignment
- [ ] Task 2.4: Step4StaffAssignment
- [ ] Task 2.5: Step5Summary

### Phase 3: Integration

- [ ] Task 3.1: page.tsx güncelle
- [ ] Task 3.2: API entegrasyonu test
- [ ] Task 3.3: State persistence
- [ ] Task 3.4: Error handling

### Phase 4: Polish

- [ ] Task 4.1: Animations & transitions
- [ ] Task 4.2: Keyboard navigation
- [ ] Task 4.3: Responsive design
- [ ] Task 4.4: Performance optimization
